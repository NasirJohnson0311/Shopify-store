import {graphql} from '@shopify/hydrogen';

/**
 * Placeholder GraphQL document to satisfy Hydrogen codegen.
 * This will be replaced with real customer account queries later.
 */
export const CUSTOMER_ACCOUNT_PLACEHOLDER = graphql(`
  fragment CustomerAccountPlaceholder on Customer {
    id
  }
`);
